<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <title>Add Game</title>
</head>
<body align="center" class="wholebody">
    <nav class="topbar">
        <div class="logo">
            <img style=" height: 3vw; line-height: 4vw;" src="/image/Logo (2).png"/>
        </div>
        <div class="menu boxhead">
            <form action="/" method="GET">
                <input style="font-size: 0.9vw; line-height: 1.8vw;" type="text" placeholder=" search game name" name="title">
            <a class="menu_items" href= "/"><span>MENU</span></a>
            <% if (session.user.role === 'admin') {%> 
            <a class="menu_items" href= "/admin"><span>addGame</span></a>
            <% }%>
            <a class="menu_items" href= "/<%= session.user.name%>/editProfile"><span>profile</span></a>
            <a class="menu_items" href= "/logout"><span>logout</span></a>

        </form>
        </div>
    </nav>
    <div class="boxhead" style="margin-top: 10%; margin-bottom: 7%; color: white;">
        <div class="gamePageDiv">
            <p style="font-size: 3vw">Welcome, Admin <%= dataAdmin.username %></p>
            <a style="background: white; color: black; font-size: 1vw; line-height: 4vw;  border-radius: 0.2vw;" href="/admin/addCategory">Add Category</a>

            <form action="/admin/addGameList" method="POST" >
                <label>Title</label><br>
                <input style="font-size: 1vw; line-height: 2vw;" type="text" name="title"><br>
                <label>Description</label><br>
                <textarea name="description" id="5" cols="30" rows="10" placeholder="enter description here ..."></textarea><br>
                <label>Image</label><br>
                <input style="font-size: 1vw; line-height: 2vw;" type="text" name="image"><br>
                Category: <select name="CategoryId">
                    <% category.forEach(list => { %>
                        <option value="<%= list.id%>"><%= list.name %></option>
                    <% }) %>
                </select><br>
                <input type="submit" value="Add Game">
            </form>
        </div>
    </div>
</body>
</html>